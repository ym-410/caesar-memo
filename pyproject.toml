[project]
name = "caesar-memo"
version = "1.0.4"
description = "A CLI memo app demonstrating Caesar cipher, password-based shifts, search, and merge sort"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
caesar-memo = "caesar_memo.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
