[project]
name = "caesar-memo"
version = "0.1.0"
description = "A Caesar cipher CLI memo app using SHA-256-derived shifts and Base64 encoding"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "uvicorn>=0.49.0",
]
