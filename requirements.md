# Caesar Memo 設計
シーザー暗号を用いてメモ本文を変換し、JSONファイルに保存するCLIメモアプリ。
パスワードからシフト数を生成し、同じパスワードを入力したときに本文を復号できるようにする。

## 使用するアルゴリズム

### シーザー暗号
メモ本文をパスワードをもとに生成したシフト数で変換する。  
メモ保存時に暗号化する。

### 2-gram
検索機能実装のために使用。  
検索文字列とメモタイトルを2文字単位に分割し、類似度を計算する。

### マージソート
2-gramによって算出された類似度をもとに、検索結果を類似度の高い順に並べる。

### パスワードからシフト数を生成
入力されたパスワードの各文字を `ord()` で数値に変換する。  
変換した数値を合計し、26で割った余りをシーザー暗号のシフト数として使用する。

## データ構造・形式
### リスト
複数のメモや検索結果を保持するために使用。

### 辞書
メモのID、タイトル、本文などをキーと値の組み合わせで管理するために使用

### JSON 
複数のメモを保存するために使用する。

## 保存形式
メモはプロジェクトルートの `notes.json` に保存する。  
`notes.json` が存在しない場合は空のメモ一覧として扱い、メモを追加したときに自動で作成する。

保存するメモは、次の情報を持つ辞書として管理する。

- `id`: メモを識別する番号
- `title`: メモのタイトル
- `body`: シーザー暗号で暗号化した本文
- `created_at`: 作成日時
- `updated_at`: 更新日時