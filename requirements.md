# Caesar Memo 設計
シーザー暗号を用いてメモ本文を変換し、JSONファイルに保存するCLIメモアプリ

## 使用するアルゴリズム
### シーザー暗号
メモ本文をパスワードをもとに生成したシフト数で変換する。  
メモ保存時に暗号化する。

### Base64
暗号化後のバイト列をJSONに保存可能な文字列形式へ変換するために使用。

### 2-gram
検索機能実装のために使用。  
検索文字列とメモタイトルを2文字単位に分割し、類似度を計算する。

### マージソート
2-gramによって算出された類似度をもとに、検索結果を類似度の高い順に並べる。

### SHA-256
パスワードのハッシュ化のために使用。  
生成したハッシュ値をもとにシーザー暗号のシフト数を決定する。

## データ構造・形式
### リスト
複数のメモや検索結果を保持するために使用。

### 辞書
メモのID、タイトル、本文などをキーと値の組み合わせで管理するために使用

### バイト列
Base64処理を行うために使用

### JSON 
複数のメモを保存するために使用する。
